{
  "frontend/src/index.css": "Global styles with deep forest green and silver OKLCH color system, Inter/Rajdhani fonts, and mobile-safe-area utilities for the service desk app.",
  "frontend/tailwind.config.js": "Tailwind config extending the theme with Inter/Rajdhani font families, green/silver color tokens, card shadows, and fade-in animation.",
  "frontend/src/App.tsx": "Main app router with all TanStack Router route definitions; static routes `/clients/new` and `/jobs/new` are explicitly registered before their dynamic counterparts, and `ClientDetailPage` is mounted for both the static and dynamic client routes.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations covering clients, jobs, parts, labor rates, signatures, Stripe (including restored useSetStripeConfiguration), damage waiver, and user management; all ID parameters use bigint consistently to match the backend interface.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header, main content area, and a fixed bottom navigation bar with five tabs: Dashboard, Clients, Jobs, Inventory (replacing Calendar), and Settings.",
  "frontend/src/pages/LoginPage.tsx": "Login screen with Internet Identity sign-in button, company branding, and logo display.",
  "frontend/src/pages/ProfileSetupPage.tsx": "Two-step profile setup page — first collects the user's name, then optionally presents the SignatureCapture component; skips the signature step if one already exists.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard page using `useIsOwner` (principal-based check) instead of role-based check for owner-only metric cards, status breakdown, and labor rates summary; inventory card visible to all users.",
  "frontend/src/pages/ClientsPage.tsx": "Clients list page with search filtering and Google Review star buttons; the \"Add Client\" button now correctly navigates to the static `/clients/new` route instead of the dynamic param route.",
  "frontend/src/pages/ClientDetailPage.tsx": "Client create/edit form that now imports `useActor` directly to track actor initialization state; shows a \"Connecting…\" banner and button label while the actor is loading, prevents mutation from firing until the actor is ready, and maps raw \"Actor not available\" errors to a user-friendly message instead of displaying them verbatim.",
  "frontend/src/pages/JobsPage.tsx": "Jobs list page grouped by status with filter tabs; the \"New Job\" button uses a named handler for clarity and navigates to the registered `/jobs/new` static route.",
  "frontend/src/pages/JobDetailPage.tsx": "Full job create/edit form with labor line items, parts, calendar scheduling, a job-level Damage Waiver toggle (off by default) that reveals an editable waiver text field when enabled, a clean Stripe payment section with only the Charge button and Paid/Unpaid badge (no explanatory text), and a Generate Estimate button.",
  "frontend/src/pages/SettingsPage.tsx": "Settings page with profile display, owner-only labor rates management, owner-only Stripe configuration, and owner-only authorized users section; fixed to use `user` (not `principal`) key in assignRole calls and passes bigint IDs directly to deleteRate.",
  "frontend/src/pages/InvoicePreviewPage.tsx": "Estimate/invoice preview page with company header, client info, line items, totals, conditional damage waiver, and signature pad; PDF export uses a print-optimised popup window instead of html2canvas/jsPDF (avoiding missing package errors), plus Download HTML, Share via Email, and Share via SMS share buttons that appear after signing.",
  "frontend/src/pages/CalendarPage.tsx": "Weekly calendar page with EST timezone formatting, expandable cost summaries per job card (diagnostic fee, labor, tax at 8.875%), and a Routing tab with drag-and-drop job ordering and Google Maps deep-links.",
  "frontend/src/pages/InventoryPage.tsx": "Full inventory management page with owner-gated CRUD; delete now passes `deleteTarget.id` (bigint) directly to `deletePart.mutateAsync` which accepts bigint, fixing the type error.",
  "frontend/src/hooks/useSignaturePad.ts": "Completely rewritten signature pad hook that registers touch listeners with `{ passive: false }` and calls `preventDefault()` on every touch event to prevent page scroll; maps touch/mouse coordinates via `getBoundingClientRect`, initializes canvas via `requestAnimationFrame`, and exposes `canvasRef`, `clear()`, `getSignatureBytes()`, and `isEmpty`.",
  "frontend/src/components/SignatureCapture.tsx": "Reusable signature capture component using the rewritten `useSignaturePad` hook; renders a canvas inside an explicit-height wrapper (`height: 160px`) with `touchAction: 'none'` and no hardcoded `width`/`height` HTML attributes so the hook controls all sizing. Includes Clear/Save/Skip buttons with loading and success states."
}